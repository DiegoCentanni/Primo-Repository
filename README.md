# Primo-Repository
DOMANDE:
2) E' un tag che simboleggia il fatto che stiamo parlando di una pagina web.
3) Il tag h1 ti fa inserire un testo che è in grassetto e maggiore a livello di grandezza di h2 che sarà a sua volta più grande di h3.
4) Il tag <p> serve per creare un paragrafo.
5) La riga <a href="/me.html">My other page</a> serve per fare in modo che quando si clicca sul testo "My other page" ti rimanderà alla pagina me.html.
7) Perchè nel file style.css abbiamo detto che il colore di background del body deve essere arancione.
8) Il tag <link rel="stylesheet" href="styles.css"> serve per collegare un file CSS esterno alla pagina HTML, così può avere colori, font e stili grafici.
10) Il selettore in un file CSS serve per scegliere a quali elementi HTML applicare uno stile.
11) Per far comparire il testo "hello" sulla console bisogna inserire il testo nel file java script scrivendo console.log("") e tra le virgolette i testo che si vuole inserire
12) Per scrivere sulla console ciao al posto di hello ho inserito "Ciao" invece che "Hello" nelle virgolette della parentesi di console.log
13) 